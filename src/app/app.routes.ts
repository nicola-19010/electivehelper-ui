import { Routes } from '@angular/router';
import { InteractiveScheduleComponent } from './interactive-schedule/interactive-schedule.component';

export const routes: Routes = [
    { path: 'horario', component: InteractiveScheduleComponent },
];
